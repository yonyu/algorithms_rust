mod arrays;
mod linked_lists;

fn main() {

    // initialize a vector
    let mut v = Vec::new();
    v.push(5);
    v.push(6);
    v.push(7);
    v.push(8);
    let _ret = arrays::two_number_sum::two_num_sum1(&v, 13);
    assert_eq!(_ret, true)
    //println!("Hello, world!");
}
